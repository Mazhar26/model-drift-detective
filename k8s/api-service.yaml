apiVersion: v1
kind: Service
metadata:
  name: drift-api-service
  labels:
    app: drift-api
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: drift-api
  type: ClusterIP
